# Consult verification — 2026-08-27

- Subject: `/fuseforge:consult` from implementation slice 9
- Machine: macOS, darwin 25.5.0, zsh
- Harnesses: Claude Code 2.1.212, Codex CLI 0.148.0, Cursor Agent
  `2026.08.25-3e8eec8`
- Install: existing skill-directory links pointing at this checkout, not a
  fresh `install.sh` round trip

## Why this slice existed

Design named Consult as a public intent: answer, compare, review, or write one
planning document, never implement. Delivery Plan phase 1 item 1 said load and
route `craft` and `consult`. Craft grew through eight slices. Consult stayed a
Skeleton file that told the reader nothing would run.

## Static

```text
FuseForge consult checks passed
FuseForge selection-gate checks passed
PASS static harness packaging
FuseForge workspace-contract checks passed
FuseForge delegation-barrier checks passed
FuseForge implement-delegation checks passed
FuseForge connected-verification checks passed
FuseForge bootstrap smoke checks passed
FuseForge install smoke checks passed
PASS release readiness metadata for 0.4.0
==> lint complete: no issues
```

`consult.md` is 135 lines, under the 200-line cap. The skeleton list in
`skills/stability.json` is empty.

## Negative tests

Six violations were injected one at a time from a copy outside Git. The
baseline was asserted to pass before each rejection, and the working tree was
restored after each test.

| Injected violation | Result |
|---|---|
| Consult status restored to Skeleton only | `still declares itself Skeleton only` |
| Implementation prohibition removed | `Never implement product behavior or change specialist source.` |
| Mode header replaced | `Begin with \`Mode: <token> \| Write permission: <none\|one document>\`` |
| Silent track/stack/topology rule removed | `Never settle a silent track, stack, or topology.` |
| Write permission widened to any documents | `Write permission: one planning document` |
| Consult moved back to the skeleton registry | `must be registered as implemented` |

The restored tree still passes.

## Live Consult probe

Each probe ran from an empty directory outside this checkout.

```text
FUSEFORGE_CONSULT_LOADED
Mode: answer
Write permission: none
```

| Harness | Command | Result |
|---|---|---|
| Claude Code | `claude -p "/fuseforge:consult FUSEFORGE_CONSULT_PROBE"` | Passed: exactly those three lines |
| Codex CLI | `codex exec --skip-git-repo-check "Use FuseForge consult: FUSEFORGE_CONSULT_PROBE"` | Passed: those three lines after loading SKILL.md |
| Cursor Agent | `cursor-agent --trust -p "FUSEFORGE_CONSULT_PROBE"` | Passed: exactly those three lines |

No probe directory contained any file afterwards.

The probe is an install check. It does not prove `consult.md` was read.

## Live Consult question

Same comparison, from empty directories:

```text
Should the shared feature contract live in the coordination root, or should
each specialist track keep its own copy?
```

| Harness | Result |
|---|---|
| Codex CLI | Passed. Loaded `workflow/consult.md` and `coordination/shared-contract.md`. User-visible answer began `Mode: proposal \| Write permission: none`. Recommended the coordination root. Wrote nothing. |
| Claude Code without extra grant | Stopped. Could not read `consult.md` from `~/.claude/skills/` and refused to improvise. Wrote nothing. This is the same non-interactive permission limit recorded for Craft, not a Consult policy miss. |
| Claude Code with `--add-dir ~/.claude/skills/fuseforge` | Mode was `proposal`, write permission `none`, recommended the coordination root, wrote nothing. It emitted two narrative sentences before the Mode header, which the policy forbids. The mode and write boundary held; the header-first rule did not. |

## Craft activation after the current install

Slice 8 probed Craft live only on Claude Code after installing. These ran
against the current skill-directory links, not a new `install.sh` round trip.

| Harness | Command | Result |
|---|---|---|
| Codex CLI | `Use FuseForge craft: FUSEFORGE_ACTIVATION_PROBE` | Passed: `FUSEFORGE_LOADED` / `Assumptions` / `Selection Gate` |
| Cursor Agent | `cursor-agent --trust -p "FUSEFORGE_ACTIVATION_PROBE"` | Passed: the same three lines |

## Not verified

- Consult `review` and `document` modes live;
- classification stability on Codex or Cursor;
- calendar Slice 2 or phase 3 coordinator scenarios;
- a live `install.sh` round trip on Linux or Windows. Isolated
  `install-smoke.sh` already runs on GitHub Actions `ubuntu-latest`;
- that every Claude Consult response will start with the Mode header. One
  granted run did not.
